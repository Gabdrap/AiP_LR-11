#include <iostream>
#include <Windows.h>

using namespace std;

int main()
{
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    int choise;
    cout << "1" "\n";
    cout << "2" "\n";
    cout << "3" "\n";
    cout << "Выберите задание" "\n";
    cin >> choise;

    if (choise == 1)
    {
        int N;
        cout << "Введите ваше число: ";
        cin >> N;
        for (N; N > N; N / N)
        {
            cout << N / N;
        }
        return 0;
    }
    if (choise == 2)
    {
        
    }
    if (choise == 3)
    {
        
        return 0;
    }

    return 0;
}
